APP_ID=com.koolcenter.fastnet
PROJECT_DIR=apps/fastnet/com.koolcenter.fastnet
FILE_PREFIX=fastnet
RELEASE_TITLE="FastNet"
HOMEPAGE_URL="https://www.koolcenter.com/t/topic/9148"
SUPPORTED_ARCH="amd64 arm64"
# 上游是 KoolCenter 自建的固件下载服务器，不是 GitHub 仓库，也没有 changelog
# 文件可切小节 —— 所以这里既不设 UPSTREAM_GITHUB 也不设 CHANGELOG_URL，
# Release 正文只会有本仓库的打包信息（reusable-build-app.yml 会自动兜底文案）。
