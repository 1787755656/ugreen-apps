APP_ID=com.personal.liteclass
PROJECT_DIR=apps/liteclass/com.personal.liteclass
FILE_PREFIX=liteclass
RELEASE_TITLE="轻课堂 LiteClass"
HOMEPAGE_URL="https://hub.docker.com/r/luodichanagn/liteclass"
SUPPORTED_ARCH="amd64 arm64"
# 上游【只发 Docker 镜像，且只有 amd64】，没有源码仓库、没有 Release、没有 tag ——
# 业务代码是 javascript-obfuscator 混淆过的 Node/Fastify 应用。
# 所以版本源是 Docker Hub 的镜像 tag（见 get-latest-version.sh）。
#
# arm64 的包是**我们补出来的**：应用本体是纯 JS（架构无关），只有 Node 运行时和
# sqlite3 的原生模块分架构 —— 这两样各自取官方对应架构的产物即可，
# 详见 build.sh。所以 amd64-only 的上游镜像能出两个架构的包。
#
# 不设 UPSTREAM_GITHUB：上游没有公开仓库（Docker Hub 页面也没填描述），
# 设了只会让 Release 正文里多一条 404 链接。同理不设 CHANGELOG_URL。
