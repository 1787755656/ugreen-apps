> ⚠ 本文写于独立仓库时期；工程现居本 monorepo（`apps/piggybank` + `scripts/apps/piggybank`）。
> 文中 `tools/ugcli` vendor 方案是独立仓库时期的做法——monorepo 里 ugcli 由 reusable workflow
> 统一下载并用 actions/cache 缓存，CDN 抖动只会让个别 run 失败重跑。

# piggy-bank 打包踩坑记录

> 从 0001 到 CI 全链路实测沉淀。面向以后打包同类应用（Node workspace 项目 / 带自带登录的第三方 / GitHub Actions 自动出包）复用。

## 一、Node 运行时树（真机 0001 翻车的根因链）

### 1. `npm ci --workspace <pkg>` 不装 workspace 根依赖
`ws` 声明在上游 monorepo 的**根 package.json**（不在 server 子包），`npm ci --workspace server --omit=dev` 只装 server 子包的依赖树——根依赖直接缺席。server 代码 `require('ws')` 在打包树里找不到模块。
**修复**：显式 `npm install ws@8.21.3 --omit=dev --ignore-scripts` 补装。
**副作用**：不带 `--workspace` 的 npm install 会按全 workspace 安装，顺带把 web 端的生产依赖（vue/@capacitor 等 32 包）拉进运行时树，还会带进 typescript（需手动 `rm -rf node_modules/typescript node_modules/.bin/tsc*`）。

### 2. 补装必须同步进真正打包的树（0001 真机炸的直接原因）
补装只发生在 staging（build/server-base），忘了拷回 rootfs 树就 pack 了 → 真机 `Cannot find module 'ws'`。
**铁律：staging 树验证 ≠ 打包树验证。所有不变量断言和冒烟必须跑在真正打进 upk 的那棵树上。** 已固化为 build.sh 断言：打包树必须有 `ws`、`better_sqlite3.node`（`file` 校验 ELF 架构）、`dist/db/schema.sql`；不得有 `typescript`；不得有悬空软链。

### 3. npm workspace 自引用软链 = pack 杀手
npm workspace 会在 `node_modules/@money-jar/server` 生成 `-> ../../server` 自引用软链。staging 布局下目标存在；拷进打包树（`app/server/node_modules/`）后 `../../server` 解析到 `app/server/server`，不存在 → 悬空 → `ugcli pack` 的 DirCopy 直接 `stat ... no such file or directory` 失败。
**修复**：构建脚本在运行时树生成后 `rm -rf node_modules/@money-jar`，并加全树悬空软链断言（`find -xtype l`）。

### 4. 冒烟跑在真实打包树上（CI 可行性）
GitHub ubuntu runner 是 x86_64 → **amd64 包可以在 CI 上直接跑打包树冒烟**（`rootfs_amd64/bin/node` 原生执行：登录/鉴权 401/静态资源/SQLite 建库全断言）。比本地 mac 冒烟（要换 darwin prebuild）更真实。arm64 包依赖 JS 同源 + ELF 静态校验 + 真机安装验证兜底。

## 二、project.yaml

### 5. 描述里写默认账号的 YAML 冒号坑
未加引号的 YAML 标量里出现**英文冒号+空格**（`Default admin account: admin`）→ `mapping values are not allowed in this context`，check 直接解析失败。中文全角冒号（：）没事。
**规避**：改写无冒号表述，或整个值加引号。

## 三、安装参数注入初始凭据（幂等模式存档；本应用最终未采用，模式可复用）

### 6. 平台约束：装完首启参数必为空
先起服务、2~3s 后才写 .env → 首启时账号已按默认值创建，安装参数"永远错过"账号创建。

### 7. 幂等应用模式（验证过的标准做法）
start.sh 每次启动用捆绑 node + better-sqlite3 + bcryptjs 检查家长账号：`bcrypt.compareSync('admin123', hash)` 为真（仍是默认密码）→ 应用参数里的账号/密码；应用内改过密 → **永不覆盖**（防止参数变更劫持账号）。装完需「停止→启动」一次。
password 型参数在原生应用 config.json 里 `encrypt: false`（.env 走明文注入的旁证）；另有密文启发式防护（≥32 位连续 base64/hex 不应用，防平台密文写坏账号导致锁死）。

### 8. 本应用最终方案
用户拍板不加参数：默认账号密码（admin / admin123）直接写进**应用介绍**。注意坑 5。

## 四、GitHub Actions

### 9. osswaf.ugnas.com 对海外 runner 不可达
绿联官方 CDN（ugcli 下载地址）从 GitHub Actions（Azure 海外）连接 300s 超时；偶尔能通是抖动，不能赌。
**修复**：ugcli 双平台二进制 vendor 进仓库 `tools/ugcli/`（构建工具入库换确定性，约 25MB）。本地 mac 跑 build.sh 记得带代理。

### 10. 构建号与 tag 策略
tag `upk-x.y.z.N` → 构建号取 N；CI 校验 tag 的 x.y.z 与 project.yaml `version` 一致。push/PR 构建号用 `run_number+100` 占位（只进 artifact 不发 Release），避免与正式序列冲突。构建号只升不降。

### 11. `gh release create` 在无 checkout 的 job 里报 not a git repository
release job 只 download-artifact 没有 .git → 显式加 `--repo "${{ github.repository }}"`。

### 12. shell `&&`/`||` 同级左结合
`[A] && { [B] && [C] || [D] && [E]; }` 实际语义是 `(([A]&&([B]&&[C]||[D]))&&[E])`——C 风格优先级直觉会让条件判死（本次 amd64 冒烟在 CI 被跳过）。**复合条件一律拆成显式标志位。**

### 13. ugcli 产物文件名的 nasync 差异
`~/bin/ugcli`（本机旧版）打出的包名带 `nasync` 中缀；官方 URL 的 1.1.0.13 不带。内容等价、均可安装，无需处理。

## 五、流程结论

- 改代码 → 走全流程重打包；只改 project.yaml → 直接 pack
- 每次出包前：打包树断言 + 冒烟（能跑则跑）+ 魔数校验，一个都不能省
- CI 冒烟绿灯 = 打包树完整性 + 应用可启动 + 自带登录可用，三重保障
